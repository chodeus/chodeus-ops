name: Stale issues & PRs

on:
  schedule:
    - cron: '0 5 * * *'  # daily
  workflow_dispatch:

jobs:
  stale:
    permissions:
      issues: write
      pull-requests: write
      actions: write
    uses: ./.github/workflows/stale-issues.yml
